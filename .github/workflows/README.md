# GitHub Actions are permanently disabled

GitHub is used only for Git and pull requests. Jenkins is the sole CI executor.
Executable `.yml` and `.yaml` workflow definitions are forbidden here now and
in the future. Historical files live under `docs/archive/github-actions` and
must never be restored, copied, dispatched, or treated as fallbacks.
